package main

import "fmt"
import "math"

	var a float64
	var b float64
	func second() {

	}
func main() {

	fmt.Print("Enter one small number please: ")
	fmt.Scan(&a)
	fmt.Print("Enter one large number please: ")
	fmt.Scan(&b)


switch  b < a {
case b < a
	func second()
	(fmt.Print("Large number must be larger than small number, try again; Enter one small number: "))
	fmt.Scan(&a)
        fmt.Print("Enter one large number: "
	fmt.Scan(&b)
	}
}

